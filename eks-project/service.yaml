apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/name: cicd-eks1
    app.kubernetes.io/instance: cicd-eks1-instance
    app.kubernetes.io/version: "1.0.0"
    app.kubernetes.io/component: backend
    app.kubernetes.io/managed-by: kubectl
  name: cicd-eks1
spec:
  selector:
    app: cicd-eks1
  type: LoadBalancer
  ports:
   -  protocol: TCP
      port: 80
      targetPort: 3000

